%h1 한국어 단어

%table.table
  %thead
    %th 
      단어
      %small.muted
        %em (Word)
    %th 
      번역
      %small.muted
        %em (Translation)
        
  %tbody
    -@words.each do |word|
      %tr
        %td=link_to word.term, word
        %td=word.translation
